# bouncy squares in raylib with c++
trying to get more used to c++ and cmake
raylib ver 5.5.0 on linux  
place raylib lib + include into project directory
make directory build
cd build
cmake ../src
make
